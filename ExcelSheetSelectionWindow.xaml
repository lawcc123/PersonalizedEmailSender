<Window x:Class="PersonalizedEmailSender.ExcelSheetSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose Worksheet"
        Width="420"
        Height="300"
        MinWidth="360"
        MinHeight="240"
        Background="#F6F7F9"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Choose recipient worksheet"
                       FontSize="20"
                       FontWeight="SemiBold"
                       Foreground="#111827"/>
            <TextBlock Text="Select the worksheet that contains the recipient list."
                       Margin="0,6,0,0"
                       Foreground="#4B5563"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <ListBox x:Name="WorksheetsListBox"
                 Grid.Row="1"
                 Margin="0,16,0,16"
                 DisplayMemberPath="Name"
                 BorderBrush="#CBD5E1"
                 MouseDoubleClick="UseSheet_Click"/>

        <Border Grid.Row="2"
                Background="#FFFFFF"
                BorderBrush="#E1E5EA"
                BorderThickness="1"
                CornerRadius="8"
                Padding="12">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Width="82"
                        Padding="12,7"
                        Margin="0,0,10,0"
                        Click="Cancel_Click"/>
                <Button Content="Use Sheet"
                        Width="96"
                        Padding="12,7"
                        Click="UseSheet_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
